# ColdLens 项目上下文与新窗口交接

> 状态：M0–M3 实验与冻结最终测试已完成，进入 M4 求职作品集收尾  
> 创建日期：2026-08-13  
> 暂定名称：ColdLens（后续可改名）

## 1. 一句话定义

ColdLens 是一个面向短视频推荐场景的多模态冷启动研究项目：当新视频缺少或完全没有用户交互时，利用封面、关键帧、标题、字幕及高层语义表示，把视频推荐给可能感兴趣的用户，并用严格的冷启动划分验证效果。

它不是“仿制抖音”，也不是一个通用 AI 平台。第一阶段的核心是可复现的算法实验和业务分析，网页演示只作为可选的最终展示层。

## 2. 为什么选择这个方向

### 2.1 对应真实问题

传统 ID 推荐依赖历史交互。新视频没有行为数据时，ID 表示难以被充分学习，容易得不到有效曝光。视频自身的视觉、文本和语义信息可以为冷启动提供内容侧信号。

### 2.2 与目标岗位的关系

- 推荐/算法岗：召回与排序、用户兴趣建模、多模态融合、冷启动、离线评测和消融实验。
- 人工智能/AI 应用岗：视觉与文本编码、VLM 语义抽取、训练和推理工程、性能与成本权衡。
- 数据分析岗：用户分群、内容分层、长尾与曝光分析、指标体系、实验设计和偏差识别。
- AI 产品经理：问题定义、目标用户、模型能力边界、指标与护栏、上线实验方案和失败管理。

目前不应声称一个项目可以同等深入地覆盖四类岗位。开发前需要确定主线，其他能力作为辅助证据。

## 3. 核心研究问题

主问题：

> 对训练阶段从未出现的新视频，加入视觉和文本内容后，能否比热门推荐、纯 ID 推荐和单模态推荐取得更好的 Top-K 推荐效果？

次级问题：

1. 视觉、文本和二者融合分别贡献多少？
2. VLM 生成的高层语义描述是否比普通视觉特征带来稳定增益？
3. 多模态方法是否只提升热门内容，还是也改善长尾覆盖？
4. 不同活跃度用户、内容类别和冷启动程度下，收益是否一致？
5. 准确率、多样性、覆盖率、延迟和成本之间如何权衡？

## 4. 第一阶段边界（MVP）

### 必须完成

1. 构造严格的新视频冷启动数据划分，保证测试新视频不进入训练交互。
2. 实现热门推荐基线。
3. 实现一个基于用户历史与视频 ID 的推荐基线。
4. 实现视觉内容推荐和文本内容推荐。
5. 实现一个简单、可解释的多模态融合模型。
6. 在同一数据划分上统一评测并做消融实验。
7. 分析模型对不同用户群、内容类别和长尾视频的影响。
8. 输出可复现代码、实验配置、结果表和失败分析。

### 第一阶段不做

- 不搭建完整短视频平台。
- 不从头训练基础 VLM。
- 不先做复杂网页、账户、部署或推荐信息流。
- 不堆叠大量模型来制造“版本进展”。
- 不用普通随机切分冒充新视频冷启动。
- 不用离线指标直接宣称真实留存、播放量或商业收益提升。

## 5. 候选数据集

### 方案 A：MicroLens 小规模子集

优势：短视频场景贴合度高，包含用户交互和视频多模态内容，适合研究内容驱动推荐。官方论文描述完整数据规模很大，因此实际应优先选择可承受的小规模版本或抽样集。

参考：[MicroLens 论文与项目入口](https://arxiv.org/abs/2309.15379)

### 方案 B：KuaiRand-Pure

优势：规模相对可控，具有多种反馈、随机曝光交互，以及视频标题/语义等补充信息；更适合去偏、因果分析和多目标反馈研究。但它是否能满足本项目需要的原始视觉内容，必须在立项时核查，不能只凭字段说明假设。

参考：[KuaiRand 官方仓库](https://github.com/chongminggao/KuaiRand)

### 初步判断

- 若主投推荐/多模态算法岗：优先核查 MicroLens 小规模版本。
- 若主投数据分析或希望研究曝光偏差：优先核查 KuaiRand-Pure。
- 第一轮只选一个主数据集，不同时维护两个大型数据管线。

数据许可、下载体积、原始视频可用性、磁盘占用和训练资源尚未完成核查，因此数据集选择不是最终结论。

## 6. 初步技术结构

```text
视频封面/关键帧 ─→ 视觉编码器 ─┐
标题/字幕       ─→ 文本编码器 ─┼→ 内容融合表示 ─┐
可选 VLM 描述   ─→ 语义编码   ─┘               │
                                                   ├→ 匹配/排序 → Top-K
用户历史序列    ─→ 用户兴趣编码 ─────────────────┘
```

第一版优先使用预训练编码器离线提取特征，避免一开始端到端训练大模型。融合先从拼接、加权或小型 MLP 等清晰基线开始；只有基线可靠后，才考虑更复杂的注意力融合或序列模型。

## 7. 建议基线与实验组

### 最低基线集合

1. Popular：按训练集热视频推荐。
2. ID-based：矩阵分解、LightGCN 或轻量序列推荐三者择一。
3. Visual-only：用户历史视觉内容与候选视频匹配。
4. Text-only：用户历史文本内容与候选视频匹配。
5. Multimodal：视觉与文本融合。

### 可选增强实验

6. VLM-semantic：使用 VLM 生成结构化描述或语义标签后推荐。
7. Behavior + Multimodal：行为表示和内容表示联合建模。

是否采用 LightGCN、SASRec 或其他模型，要根据数据结构和显卡决定，不在未看数据前固定。

## 8. 数据划分与防泄漏要求

至少建立以下测试条件：

- Warm：用户和视频均在训练阶段出现，用作常规性能参照。
- Item Cold：测试视频在训练交互中完全未出现，是本项目的主评测集。
- Few-shot Item：视频只有极少交互，用于观察冷启动过渡阶段。
- Temporal：条件允许时按时间切分，避免使用未来交互预测过去。

必须检查：

- 测试视频的交互不得进入训练。
- 由全量交互统计出的热门度、标签或聚合特征不得泄漏测试信息。
- 同一视频的重复记录、近重复片段或派生内容不得跨集合造成隐性泄漏。
- 负样本采样方法必须写清楚，并保持模型间一致。
- 模型选择使用验证集，最终测试集不能反复用于调参。

## 9. 指标体系

### 推荐效果

- Recall@K
- NDCG@K
- HitRate@K
- 可选：MRR@K

### 产品与生态指标

- Catalog Coverage / 内容覆盖率
- Intra-list Diversity / 列表内多样性
- Long-tail Exposure / 长尾曝光占比
- Novelty / 新颖度（需明确定义）

### 工程指标

- 单视频特征提取耗时
- 单用户 Top-K 推理延迟
- 峰值显存和内存
- VLM/API 成本（如使用外部服务）

离线指标只是代理指标。没有线上随机实验时，不能把 Recall 或 NDCG 的提升解释成真实留存或商业收益提升。

## 10. 数据分析与产品分析任务

模型训练之外，至少回答：

1. 数据中的用户活跃度和视频受欢迎度是否呈长尾分布？
2. 新视频在不同类别中的数量与互动差异是什么？
3. 多模态增益集中在哪些用户群和内容类别？
4. 高活跃用户和低活跃用户的收益是否不同？
5. 准确率提升是否伴随覆盖率或多样性下降？
6. 若上线，主指标、护栏指标和异常监控应如何设置？

可选产品实验设计：提出 A/B 实验方案，包括随机化单位、主指标、护栏指标、SRM 检查、MDE/样本量和实验周期。这里只设计方案，不伪造线上实验结果。

## 11. 交付物

最终理想交付物：

- 一套可复现的数据处理与训练代码。
- 固定的数据切分和实验配置。
- 基线、融合模型及消融实验结果。
- 一份数据与业务分析 Notebook。
- 一份正式实验报告，包含失败案例和限制。
- 一张系统结构图和一张实验结论图。
- 可选：极简演示页，展示某用户为何收到若干新视频推荐。
- 面向不同岗位的两种项目表述，而不是一份简历描述硬投所有岗位。

## 12. 主要风险

1. 数据风险：原始视频、多模态字段或许可不满足需要。
2. 算力风险：视频特征提取和序列模型超出本地资源。
3. 定义风险：数据划分不严格，结果并非真正冷启动。
4. 对比风险：复杂模型没有超过简单内容相似度基线。
5. 产品风险：离线增益无法证明真实用户价值。
6. 范围风险：同时追求算法、分析、产品和完整前端，导致各部分都不深入。

风险控制原则：先检查数据与算力，再写训练代码；先建立简单基线，再增加 VLM；发现研究问题不成立时允许调整或终止，而不是靠增加功能掩盖。

## 13. 初步里程碑（待资源确认后定日期）

### M0：立项核查

- 确定主投岗位。
- 确认显卡、显存、内存、磁盘和每周时间。
- 下载并抽查候选数据集。
- 写清冷启动定义和数据许可边界。
- 给出继续/停止结论。

### M1：数据与基线

- 完成数据审计、统计分析和严格切分。
- 跑通 Popular 与一个 ID-based 基线。

### M2：多模态模型

- 提取视觉和文本特征。
- 完成单模态与简单融合模型。
- 统一评测并记录成本。

### M3：VLM 与深入分析

- 仅在有明确假设时加入 VLM 语义特征。
- 完成消融、分群、长尾、多样性和错误分析。

### M4：作品集收尾

- 固化实验。
- 撰写报告、简历描述和演示材料。
- 只在确实能帮助表达时做极简 Demo。

## 14. 开发前必须回答的问题

1. 你的显卡型号和显存是多少？CPU、内存、可用磁盘是多少？
2. 每周能稳定投入多少小时，期望几周完成？
3. 主投优先级是什么：推荐算法、AI 应用算法、数据分析还是 AI 产品经理？
4. 更看重求职作品、毕业课题、论文潜力还是产品创业验证？
5. 是否接受下载较大的公开视频数据？是否愿意使用付费 API？
6. 你的 PyTorch、推荐系统、SQL 和统计实验基础分别如何？

这些答案会影响数据集、模型复杂度和项目周期。未回答前不应直接开始下载大数据或搭平台。

## 15. 新窗口应立即执行的下一任务

新窗口先完整读取本文件，然后：

1. 用简短语言复述项目定位、当前状态和关键风险。
2. 收集第 14 节的资源与求职约束。
3. 在线核查 MicroLens 与 KuaiRand 当前可下载内容、许可、体积和多模态字段。
4. 基于核查结果只推荐一个主数据集和一个备用方案。
5. 输出 M0 立项核查清单与预计资源成本。

在完成上述核查前，不创建训练框架、不下载大型数据、不设计网页，也不制造多个方案版本。

## 16. 当前事实边界

- 已确定：用户对“多模态短视频冷启动推荐”最感兴趣，希望将其作为下一个项目候选。
- 已确定：项目不涉及 VLA 或机器人硬件。
- 已确定：先做规划，新窗口再继续讨论，当前未授权进入实现。
- 暂定：ColdLens 名称、技术结构、基线集合和里程碑。
- 未确定：主投岗位、数据集、硬件资源、周期、模型选型、API 预算和是否做 Demo。

## 17. 2026-08-13 M0 更新

### 已确定

- 主投岗位：AI 产品经理。
- 项目目的：求职作品。
- 时间投入：每天至少 3 小时。
- 主数据集：MicroLens-50K；备用数据集：KuaiRand-Pure。
- 第一版模态：标题与封面，不使用完整视频、关键帧包或音频。
- 本地优先开发；公司服务器只用于经本地验证且确有必要的 GPU 任务，不作为长期数据仓库。
- 公司服务器任务完成后，先核验结果已回传本地，再清理明确的个人临时目录。

### 本地资源

- CPU：Intel Core i5-12500H，12 核 16 线程。
- 内存：16GB。
- GPU：Intel Iris Xe，无本地 CUDA GPU。
- D 盘可用空间约 277GB，项目根目录为 `D:\ColdLens`。
- Git 安装于 `D:\Apps\Git`；项目虚拟环境为 Python 3.12.10，位于 `D:\ColdLens\.venv`。

### 数据审计结论

- 已下载约 14.5MB MicroLens-50K 元数据，未下载任何媒体文件。
- 数据包含 359,708 条评论交互、50,000 用户和 19,220 视频；标题覆盖完整，CSV/TSV 序列一致。
- 80%/90% 全局时间切分下，验证集有 1,305 个训练未见视频，测试集有 1,008 个此前未见视频，严格模型侧 Item Cold 在样本量上可行。
- 关键边界：交互来自公开评论，不是点击或观看；离线结果只能解释为高意图评论行为匹配，不能宣称提升 CTR、播放时长或留存。
- 第一次观测到评论不等于视频上传时间，因此不能把实验包装成真实平台新发布视频实验。
- 全局点赞/播放统计可能包含未来信息，不得进入训练特征或公平基线。

详细审计见 `docs/M0_DATA_AUDIT.md`。

### 已冻结评测协议

- 协议版本：`split_v1`。
- 开发阶段使用前 80% 时间交互训练，并在 80%–90% 时间窗的严格冷视频上验证。
- 超参数冻结后，最终阶段使用前 90% 时间交互重新训练，并在最后 10% 时间窗的严格冷视频上测试。
- 最终测试冷候选池包含 1,008 个视频，采用全候选排序，不做采样负例。
- 最终测试包含 18,812 个用户—冷视频正例，覆盖 13,810 名有最终训练历史的用户。
- Cold 集合报告 Random、ID/UNK 退化、Text-only、Visual-only 和 Multimodal；Popular 与正常 ID-based 模型主要在 Warm 参照集报告。
- 详细口径见 `docs/EVALUATION_PROTOCOL.md`，冻结配置见 `configs/split_v1.json`。

### 首个模型验证结果

- 已完成 `text_tfidf_validation_v1`，仅运行 Validation Cold，未读取最终 Test Cold。
- 标题词表只从开发训练视频拟合；用户画像为历史评论视频标题向量的归一化汇总。
- Validation Cold：Recall@10 = 0.12797，NDCG@10 = 0.08211，HitRate@10 = 0.15636。
- 同一协议的单次 Random 烟雾测试 Recall@10 = 0.00709；TF-IDF 显示出明显标题兴趣信号。
- 精确重复标题不足以解释主要增益：仅 42 / 18,508 个正例与对应用户历史标题完全一致。
- 该结果仍只代表评论行为匹配，不代表点击、观看、留存或线上因果效果。
- 下一步先做验证集错误分析，再决定是否下载封面。

### 标题模型错误分析结论

- 历史越丰富，验证 Recall@10 越高：1–3 条历史为 0.0841，11 条以上为 0.2123；该关系可能同时受用户活跃度与兴趣稳定性混杂，不能解释为因果。
- Jaccard ≥ 0.8 的近重复标题正例仅 60 / 18,508；去除后 Recall@10 为 0.12478，主要增益不是由少数近重复样本造成。
- 2,350 / 18,508 个正例与用户历史没有有效词汇相似度，跨表达、中文标题和视觉信息缺失是主要盲区。
- 已决定进入最小 Visual-only 阶段：下一步只下载约 0.62GiB 封面包并先做质量审计，不下载关键帧、视频或音频。

### 封面数据审计结论

- 已下载官方封面 ZIP，大小 668,400,742 字节，SHA-256 为 `135255149CC74D47F1FB04985B2D16B862EE3D00C963FF47CB756B2136CA5892`。
- 19,220 张 JPEG 与视频 ID 完整一一对应；CRC、路径、文件名和 JPEG 尺寸头检查均通过。
- 字节级完全重复共 10 组，没有跨越训练—冷候选边界。
- dHash 筛查发现验证冷候选 21 张、测试冷候选 13 张与对应训练封面哈希相同；人工抽查表明其中混有真近重复、同模板和明显误报。
- 冻结敏感性规则：保留 `split_v1` 主结果，另报告排除跨边界 dHash=0 冷候选的 `visual_clean_dhash0`；所有模型在清洁集合上使用同一候选池。
- 封面审计通过。下一步先在本地抽样验证轻量视觉编码流程，再决定是否使用公司服务器完整提取。

### 视觉编码冒烟测试结论

- 已用固定 32 张图片验证 ZIP 直读、解码、缩放、固定维度向量和余弦相似度链路。
- 零依赖 8×8 RGB 网格能识别相同画面，但会把不同主题、相似版式的图片判为高度相似，因此不能作为正式 Visual-only 基线，也不进行全量提取。
- 已在 D 盘隔离环境安装 `torch 2.10.0+cpu` 与 `torchvision 0.25.0+cpu`；虚拟环境约 636MiB，核心离线 wheel 约 112.5MiB，未占用公司服务器。
- 官方 MobileNetV3-Small ImageNet 权重的 576 维全局池化特征通过固定 32 张冒烟测试：重复推理最大绝对差为 0；同画面相似度 0.99908，两组 dHash 明显误报降至 0.55522 与 0.49688。
- 512 张流式基准约 93.9 张/秒，证明本地全量可行；32 张小样本的 13.6 分钟外推受固定开销影响，不能作为最终容量估计。
- 已在本地 CPU 完成 19,220 张封面的全量提取，耗时 235.49 秒，实测约 81.6 张/秒；输出为 `19220 × 576` float32 单位向量。
- 全量 ID 与封面审计表完整一致；前 512 行与独立基准逐值相同。向量 SHA-256 为 `58430b56a5c4f62a5a451d0b52b221ae74e0810ad0f6de71bae2f958b9ddc3ac`，item ID SHA-256 为 `c96d643ce34f69a8f2283811dff9b1af6d7d6bbe5dbe9e766ed228dbd8b42944`。
- 当前仍无需公司服务器。下一步只在 Validation Cold 上建立 Visual-only 推荐基线；在看到验证指标前，不能把视觉相似度行为解释成推荐效果。

### Visual-only 验证结果

- 已完成 `visual_mobilenet_v3_small_validation_v1`，只读取开发训练与 Validation Cold；模型脚本未读取最终训练或测试集合。
- 用户画像为历史封面 576 维单位向量的等权求和并归一化；候选按余弦相似度做 1,305 个全候选排序，无推荐标签训练。
- Validation Cold：Recall@10 = 0.04253，NDCG@10 = 0.02365，HitRate@10 = 0.05549；明显高于 Random 烟雾量级，但显著弱于 Text TF-IDF 的 Recall@10 = 0.12797。
- Visual Recall@10 约为 Text 的 33.2%，Recall@50 约为 Text 的 53.1%；视觉信号存在，但当前简单视觉画像的头部排序较弱。
- `visual_clean_dhash0` 排除 21 个候选、108 个正例及 58 名失去全部正例的用户后，Recall@10 = 0.04261、NDCG@10 = 0.02362，主结论不依赖冻结规则中的 dHash=0 可疑候选。
- 历史 1–3 条用户 Recall@10 = 0.03162，11+ 条用户为 0.06474；这是相关性，不作因果解释。
- 两次完整评测除运行时间外逐字段一致。下一步先检查 Text/Visual 用户级命中互补性，再决定简单融合形式和验证集权重搜索范围。

### Text–Visual 排名融合结果

- 在查看融合结果前冻结：按用户内候选排名加权；Text 权重只检查 `0、0.25、0.5、0.75、1`；以 Validation NDCG@10 为主选择指标、Recall@10 为并列指标；清洁集合不重新调权。
- 联合脚本精确复现既有 Text、Visual 和清洁 Visual 端点；两次完整运行除耗时外逐字段一致。
- Text 与 Visual Top-10 平均只重叠 0.332 个候选；417 名用户两者都命中，1,733 名仅 Text 命中，346 名仅 Visual 命中，11,254 名均未命中。
- Text 或 Visual 任一命中的 oracle 上界覆盖 2,496 名用户，约 0.18153 HitRate@10，高于 Text 的 0.15636；这只证明存在互补空间，不是可实现模型结果。
- 五档全局 rank fusion 的最佳 Validation NDCG@10 出现在 Text 权重 1.0、Visual 权重 0.0，即退化为 Text-only；任意已检查的正视觉权重都降低 Recall/NDCG/HitRate@10。
- 当前不得宣称多模态融合提升。下一步只允许先分析无需测试标签的文本置信度能否识别“仅 Visual 命中”用户，再预先冻结一个条件式门控实验；若没有稳定信号，则保留 Text-only，并将视觉分支作为负结果。

### 条件式模态门控结果

- 在运行前冻结用户拆分、唯一置信度特征、六档切换比例、选择指标和接受条件。13,750 名 Validation 用户按稳定 SHA-256 奇偶拆成 gate-dev 6,781 人、gate-audit 6,969 人。
- 门控特征为用户对完整冷候选池的最高 TF-IDF 余弦分数；低于 gate-dev 分位数阈值时整套切换到 Visual 排名。
- gate-dev 最佳切换比例为 0%。10%视觉切换已使 Recall@10 从0.13985降至0.13761、NDCG@10从0.09036降至0.08896，之后随切换比例增加继续下降。
- gate-dev 最低文本置信度的679名用户中，仅 Visual 命中17名、仅 Text 命中35名、两者均未命中623名；低最高分没有识别 Visual 更可靠的人群。
- 因0%从 gate-dev 晋级，gate-audit 冻结门控等于 Text-only，不能满足两项指标都严格提升的接受条件。门控被拒绝，当前轻量基线范围保留 Text-only。
- gate-dev 与 gate-audit 的 Text Recall@10 分别为0.13985和0.11642，提示用户拆分难度不完全平衡；只解释组内配对变化，不把内部 audit 当成独立时间测试。
- 若继续追求多模态提升，必须升级为新的学习式模型阶段并单独冻结验证预算；不得继续在同一验证结果上追加启发式门控。最终测试模型指标仍未运行。

### 学习式多模态晚融合结果

- 训练协议在运行前冻结：每名至少2条开发训练交互的用户以最后一次交互作正例，画像只含此前历史；每个正例从目标时间前已观测、且用户从未互动的视频中确定性采样5个负例。
- 48,422名用户产生242,110条配对；43,522名内部训练用户、4,900名calibration用户。配对文件约7.0MB，最小合法负例池566；无目标自包含或负例时间违规。
- 三个标准化特征为Text cosine、Visual cosine和两者乘积；只学习三个softmax权重。内部calibration选择第18轮，最终权重约0.77085、0.21695、0.01219；训练两次完全复现。
- 内部calibration严格配对准确率：Text 0.60065、Visual 0.61433、学习式融合0.65420。该改善只针对均匀负采样代理任务。
- 唯一冻结版本在外层Validation Cold：Recall@10由Text的0.12797降至0.12717，NDCG@10由0.08211升至0.08283；Recall@20由0.16486升至0.16931，Recall@50由0.22417升至0.23316。
- dHash0清洁集合方向一致：融合Recall@10低于Text，NDCG@10及更深K优于Text。两次外层评测除耗时外逐字段一致。
- 运行前接受条件要求Recall@10与NDCG@10都严格提升，因此学习式融合被拒绝，最终候选保留Text-only；不得事后改成只看NDCG。
- 配对构建约5.7秒、训练约9秒、外层评测约56秒，全部本地CPU完成，公司服务器无必要。
- `docs/MODEL_FREEZE_V1.md` 已冻结一次性最终测试范围：运行Text-only最终候选和Visual-only独立基线；不运行三个已被Validation拒绝的融合模型测试指标。当前仍未运行最终模型Test。

### 冻结最终测试结果

- 一次性最终测试已于2026-08-14按`docs/MODEL_FREEZE_V1.md`执行并锁定；同一冻结实现做一次确定性复现，除耗时外逐字段一致。
- 使用323,736条final_train交互重建Text模型；Test Cold包含1,008个完整候选、13,810名用户和18,812个正例。
- Text-only Test：Recall@10 = 0.12551，NDCG@10 = 0.07969，HitRate@10 = 0.15214；Recall@50 = 0.23490。
- Visual-only Test：Recall@10 = 0.04543，NDCG@10 = 0.02800，HitRate@10 = 0.05800；Recall@50 = 0.13652。
- Text在所有冻结K和指标上均明显优于Visual。Validation与Test候选数和分布不同，不能把直接差值完全解释为泛化变化；可确认相对结论一致且Text没有数量级失效。
- dHash0清洁Test排除13个候选、108个正例和63名失去全部正例的用户；Text Recall@10/NDCG@10为0.12601/0.07997，Visual为0.04585/0.02818，主结论不变。
- 未运行Validation已拒绝的全局rank fusion、条件门控和学习式融合Test指标，不根据Test翻案。
- 最终结果只解释公开评论正反馈匹配，不代表CTR、观看时长、留存、曝光或线上因果提升；第一评论时间不等于上传时间。
- 完整结果见`docs/FINAL_TEST_RESULTS.md`，入口为`scripts/evaluate_frozen_final_test.py`。测试后模型与指标口径已锁定。

## 18. 2026-08-14 M4 作品集包装

- 已新增仓库入口 `README.md`，面向招聘方概括问题、角色、关键结果、产品判断、可信度边界与复现入口。
- 已新增 `docs/PORTFOLIO_CASE_STUDY.md`，以 AI 产品经理视角记录问题重定义、实验去留、工程 ROI、线上验证草案与风险清单。
- 已新增 `docs/REPRODUCIBILITY.md` 与 `requirements.txt`；依赖版本来自本地已验证环境，数据与产物仍不进入 Git。
- 已新增 `docs/assets/final_test_metrics.svg`，展示冻结 Test Cold 的 Text-only 与 Visual-only 核心对比；图表已做实际渲染检查。
- 相对文档链接检查通过；`.venv`、数据、封面、特征与输出继续由 `.gitignore` 排除。
- 项目仍未创建提交；是否建立公开仓库、提交或推送必须由用户另行决定。
- 模型与最终 Test 继续保持锁定。M4 只整理现有证据，不产生新模型指标，也不把离线结果包装成线上业务提升。

### 数据与产品分析

- 已预先冻结 `docs/PRODUCT_ANALYSIS_PROTOCOL.md`，再运行 `scripts/analyze_product_metrics.py`；只做全量描述统计与 Validation Cold 诊断，未读取 final_train、Test 标签或最终报告，也未选择新模型。
- 视频互动长尾强于用户：视频/用户 Gini 分别为 0.53365/0.21570；头部 20% 视频/用户分别贡献 56.08%/35.65% 互动。
- Text Validation Recall@10 随历史分组从 1–3 条的 0.08411 上升到 11+ 条的 0.21226；Visual 从 0.03162 上升到 0.06474。该差异只作相关性解释。
- Text/Visual Catalog Coverage@10 分别为 99.54%/91.03%；头部 10% 候选分别占 42.59%/67.67% Top-10 槽位，Visual 曝光更集中。
- 标题审计发现 43 条在当前英文词法下为零 token，少数异常长文本最高 1,232 token；不能把结果无条件外推到真实平台短标题。
- 正式报告为 `docs/PRODUCT_ANALYSIS.md`，图表为 `docs/assets/interaction_concentration.svg` 和 `docs/assets/history_segment_recall.svg`。
- 机器可读输出为 `outputs/analysis/product_analysis_v1.json`（Git 忽略），SHA-256 为 `502adabeb4e821ea13ae41a666cba3ed5a3d1abc4015fa150549686a97f42321`；连续两次运行与图表均逐字节一致。

### 系统方案

- 已新增 `docs/SYSTEM_DESIGN.md`，明确区分“已实现离线系统”“线上提案”和“尚待实测”三类状态。
- 文档包含现有审计—切分—特征—Validation—冻结 Test 链路，以及未来内容接入、用户画像、冷启动分流、混排、日志与监控链路。
- 当前线上提案保留 Text-only 作为有历史用户的冷启动兜底；Visual 只保留为离线证据，不进入主链路。
- 4+ 条历史是基于 Validation 分层形成的首轮实验假设，不是已验证生产阈值；1–3 条历史走低置信与探索组合，零历史必须使用独立新用户策略。
- 已补充标题/画像/版本/超时/曝光集中/SRM 的故障与降级矩阵，以及数据质量、服务健康、模型生态四层监控。
- 已记录真实本地离线资源，但不伪造在线 P99、QPS、SLO 或单位流量成本；这些必须在真实原型和压测后确定。
- 两个 Mermaid 架构块结构检查通过；全部资源数字与现有 JSON/特征文件对账通过，文档相对链接与隐私字符串检查通过。
- 下一阶段为本地只读 Demo：只消费已有数据与冻结结果，展示用户历史、冷候选推荐、解释和产品边界；不得重新训练或生成新 Test 指标。

### 本地只读 Demo

- 已先冻结 `docs/DEMO_PROTOCOL.md`，再实现 `scripts/export_demo_data.py`；脚本只读取 dev_train、Validation Cold、Validation 候选和标题，不读取 final_train、Test 或最终报告。
- 每个 `1–3`、`4–6`、`7–10`、`11+` 历史分层按稳定 SHA-256 顺序选择一个 Text Top-10 命中和一个未命中用户，共 8 个匿名样例；该平衡样例不代表总体命中率。
- 本地样例位于 `demo/public/demo-data.json`，不包含原始 user ID，标题最长截断到 180 字符；文件被 Git 忽略，不随仓库发布。
- 已在 `demo/` 实现中文单页只读应用：可切换 8 个匿名用户，查看最近历史、Validation 正例、Text Top-10、匹配词、余弦相似度及产品分流/降级说明。
- 页面明确提示相似度不是概率、评论不是曝光/点击/观看标签、平衡样例不是总体效果；前端不读取原始 CSV、封面、视觉特征或 Test。
- Demo 使用 Node.js 22+ 和本地 vinext/React 项目；依赖与构建产物均位于 D 盘并被 Git 忽略，没有部署到公网。
- `npm run build` 成功；结构/数据边界测试通过；本地页面与样例接口均返回 HTTP 200。
- 初始化器创建的 `demo/.git` 已移到可恢复的忽略目录 `.tmp/demo-nested-git-backup`，使外层 ColdLens 仓库可以统一管理 Demo；没有删除页面代码或数据。

### 模拟用户模式

- Demo 已扩展为“实验样例 / 创建模拟用户”双模式。创建模拟用户默认允许从匿名演示目录选择 1–6 条训练期历史视频，使用正式实验同一“历史向量求和后 L2 归一化”公式，并在浏览器内对完整 1,305 个 Validation 冷候选重新排序。
- 历史演示目录按动漫游戏、美食、萌宠、运动、音乐舞蹈、影视剧情 6 个主题各稳定抽取 60 条，共 360 条；不含原始 item ID。该主题平衡目录用于机制演示，不代表训练目录真实分布。
- 每条推荐显示余弦相似度最高的一条已选历史作为“主要相关历史”；这是文本相似度解释，不是因果归因。
- 原兴趣词近似模式保留为次级入口，除 6 个预设外新增 6 类共 58 个可点击兴趣词，并允许英文/数字自由输入；页面显示已识别词、未识别词、匹配词和余弦相似度。58 个词均由导出脚本确认存在于冻结 IDF 与候选词表，但不代表模型完整词表。
- 模拟输入不保存、不调用 API；没有未来真实行为标签，因此不显示命中/未命中，不计算 Recall/NDCG/HitRate，也不写入原实验。
- 当前冻结 tokenizer 只可靠识别英文和数字；中文或非 ASCII 输入会明确显示未识别并不生成伪推荐。支持中文必须更换编码器并重新建立独立评测。
- 导出文件包含本地候选稀疏向量、候选标题、IDF、360 条历史目录、58 个兴趣词和 Python 参考场景，仍被 Git 忽略；最新 SHA-256 为 `59369de973a130015fc02c474be451380465014ff1ba33935d18765a29518aee`，大小约 2.63MB。
- Node 测试确认历史组合的浏览器画像、Top-10 和主要相关历史均与 Python 正式口径参考结果一致；全部 6 个兴趣词预设、零有效词和中文输入边界检查也通过。

### 方案对比模式

- Demo 已新增第三个“方案对比”模式，集中展示 Text-only 与 Visual-only 的冻结 final Test Recall@10、NDCG@10、HitRate@10，以及当前选择 Text-only 的产品取舍。
- 因用户反馈不理解 Text/Visual，页面顶部已补充新手解释：Text 看历史视频标题，Visual 看历史视频封面；使用同一个动漫推荐例子展示两条处理路径，并明确当前交互 Demo 使用标题文本方案。
- 页面说明全局排名融合、低文本置信门控和学习式晚融合分别为何在 Validation 被拒绝；学习式融合只改善 NDCG@10、同时降低 Recall@10，因此未通过事前双指标门槛。
- 对比页只包含与正式报告对账的只读汇总常量，运行时不读取 Test 标签、`final_train`、最终报告或模型产物，也没有为三个已拒绝融合补算 Test。
- 页面明确区分“当前 Visual 基线未通过产品门槛”和“视觉永远无用”，并强调公开评论离线排序差值不能换算为 CTR、观看时长、留存或线上因果提升。

### 面试讲解路线

- 已新增 `docs/INTERVIEW_WALKTHROUGH.md`，按 0–5 分钟组织问题、数据纠偏、实验可信度、Text/Visual 区别、结果取舍、Demo 操作和上线缺口。
- 文档包含 3 分钟压缩版、三个 Demo 模式的操作顺序、10 个常见追问、数字小抄和禁止表述。
- 讲解明确要求不要把评论说成点击、不要把首次评论说成上传时间、不要声称线上 CTR/留存提升，也不要把当前模块包装成完整抖音式平台。
- 对 AI 协作保持诚实：只能陈述本人真正理解和能够负责的工作，不声称独立完成不理解的代码细节。

### 简历项目描述

- 已新增 `docs/RESUME_PROJECT_DESCRIPTION.md`，面向 AI 产品经理岗位提供四条标准版、三条精简版、招聘平台版和一句话版本。
- 简历重点放在数据前提纠偏、冻结评测协议、模型准入/淘汰、工程 ROI 和线上验证设计，不把复杂模型数量当作成果。
- 标准版明确写明“使用 AI 辅助完成实验实现与结果核验”，避免声称独立开发完整推荐系统；代码能力只陈述本人真正理解且能够解释的部分。
- 禁止使用点击历史、真实新发布、CTR/留存提升、多模态已上线和完整抖音系统等不受证据支持的表述。

### 项目最终验收

- 已新增 `scripts/start_demo.ps1`：检查 Python、Node.js 22.13+、匿名样例和前端依赖；支持 `-Setup`、`-Regenerate`、`-NoBrowser` 与 `-CheckOnly`，默认在前台启动并允许 Ctrl+C 停止。
- 已新增 `scripts/check_project_readiness.py` 与 `docs/PROJECT_ACCEPTANCE.md`；自检不训练模型，也不读取 Test 标签。
- 自动验收通过 10 类检查：必需文件、UTF-8 JSON、25 份 Markdown 链接、Python 语法、冻结最终报告哈希、产品分析哈希、Demo 匿名边界、嵌套 Git、关键忽略规则和 5MiB 公开文件上限。
- Demo 一键启动预检通过，Node.js 版本为 22.20.0；最终前端构建与结构测试通过。
- 当前 `ready_for_local_use = true`；首次本地提交已获用户授权。公开发布仍未就绪，因为尚未选择代码许可证，也未授权创建公开仓库。
- 已新增 `.editorconfig` 与 `.gitattributes`，统一 UTF-8、换行和常用文件类型处理。
- 用户选择跳过模拟面试，简历项目描述已完成；首次本地提交使用用户提供的Git身份。下一步只剩许可证选择和是否创建公开仓库，均需用户另行明确授权。
